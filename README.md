# setup

setup base environment

* tmux
* redis
* cpanm
* mysql(MariaDB)
* nginx

# Usage

```
mkdir /var/www && cd /var/www
git clone git@github.com:Andryu/setup.git
cd setup
./installer.sh
```

# History
